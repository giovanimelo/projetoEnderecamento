@model WebService.Models.Enderecamento


@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_mainView.cshtml";
}

<section class="content">
    <div class="row">
        <div class="col-xs-12">

            <div class="box">

                <div class="box-header">
                    <div class="form-horizontal">
                        <h4 class="box-title">Deseja mesmo excluir esse endereço?</h4>
                        <div>
                            <hr />
                            <dl class="dl-horizontal">
                                <dt>
                                    @Html.DisplayNameFor(model => model.produto.descricao)
                                </dt>

                                <dd>
                                    @Html.DisplayFor(model => model.produto.descricao)
                                </dd>

                                <dt>
                                    @Html.DisplayNameFor(model => model.endereco.descricao)
                                </dt>

                                <dd>
                                    @Html.DisplayFor(model => model.endereco.descricao)
                                </dd>

                            </dl>
                            @using (Html.BeginForm())
                            {
                                @Html.AntiForgeryToken()
                                <div class="box-footer">
                                    <div class="pull-left">
                                        @Html.ActionLink("Voltar", "Index")
                                    </div>

                                    <button type="submit" value="Delete" class="btn btn-danger pull-right">Deletar</button>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
